---
title: Codex 数据质量筛选
date: 2026-06-19
tags:
  - codex
  - ai-training
  - data-quality
  - llm
aliases:
  - Codex 训练数据质量判断
---

# Codex 数据质量筛选

## 背景

Codex 能在本地用 JSON 或 rollout 保存对话与工具轨迹，但本地保存不等于一定会被上传或用于训练。是否上传、是否用于训练，取决于产品设置、账号类型、遥测/反馈机制和 OpenAI 当前政策。

如果假设有大量 agent 轨迹，关键问题不是有没有数据，而是如何稳定地区分高质量轨迹和低质量轨迹。人工逐条阅读不现实，也难以保证标准一致。

## 核心判断思路

不要直接把原始对话全量当作好数据。更合理的是用结果信号、程序验证、自动评测、少量人工 gold set 和用户反馈组合筛选。

## 结果信号

好信号包括：

- 用户接受了修改
- PR 最终合并
- 测试通过
- lint、format、typecheck 通过
- CI 通过
- patch 能 clean apply
- 用户没有继续要求修同一个问题
- 后续没有 revert
- 工具调用成功，路径、命令、文件真实存在

坏信号包括：

- 用户打断、撤销或明确说不对
- 测试失败
- 编译失败
- patch apply 失败
- 修改了无关文件
- 误删或破坏工作区
- 编造不存在的 API、文件、命令
- 重复尝试失败命令
- 审批被拒绝
- 违反权限或安全策略

这些信号是弱标签，不完美，但比人工逐条判断更稳定。

## 自动评测

可以用另一个模型或专门 judge 对轨迹打分，但问题要拆成具体维度：

- 是否准确理解用户请求
- 是否读取足够上下文
- 是否遵守仓库规则
- 是否最小化改动
- 是否保留用户已有改动
- 是否运行合适测试
- 最终说明是否诚实
- 是否存在幻觉或过度承诺

对代码任务，judge 可以只看用户请求、diff、测试结果和最终回答，而不是依赖完整主观阅读。

## 程序化验证

能用机器判的优先用机器判：

- 单元测试、集成测试、CI
- typecheck
- lint
- formatter
- snapshot diff
- patch apply
- API schema 兼容性
- benchmark 回退
- 安全策略
- 文件存在性
- JSON schema

这类信号通常比模型 judge 更稳定。

## 轨迹切片

一条会话中可能前半段错误、后半段修正后正确。因此训练数据不应简单按整条会话保留或丢弃，而应切片：

```text
用户请求
+ 必要上下文
+ 好的中间行动
+ 正确工具调用
+ 最终成功结果
```

坏的部分可以丢弃，也可以作为负样本训练模型识别错误策略。

## 用户反馈

高置信用户信号包括：

- thumbs up/down
- 明确评价
- 接受或拒绝建议
- 应用 patch
- 关闭问题
- 继续追问同一错误

用户反馈通常稀疏，但质量高，适合作为高置信样本。

## 人工审核

人工审核不适合全量数据，但适合：

- 校准自动 judge
- 制定 rubric
- 审核边界案例
- 评估筛选器误判率
- 构建 gold set

人工只需要看极小比例样本，但这批样本要高质量。

## 数据清洗

海量 agent 数据还需要处理：

- 去重
- PII 和 secrets 过滤
- license 过滤
- train/eval contamination 过滤
- 仓库或用户级隔离
- 长上下文裁剪

否则数据量越大，噪声和风险也越大。

## 多维评分

更合理的是多维打分，而不是简单好坏二分类：

```yaml
task_success: 0.8
code_correctness: 0.9
instruction_following: 0.7
safety: 1.0
efficiency: 0.6
user_satisfaction: unknown
test_verified: true
human_review_required: false
```

不同训练目标可以选择不同子集。

## 结论

对 Codex 这类 coding agent，最有价值的数据不是说得漂亮的回答，而是从用户请求、上下文读取、工具调用、代码修改到测试通过的完整成功轨迹。

可靠的数据筛选应组合：

- 结果信号
- 程序验证
- 自动评测
- 用户反馈
- 少量人工 gold set
- 隐私和污染过滤
